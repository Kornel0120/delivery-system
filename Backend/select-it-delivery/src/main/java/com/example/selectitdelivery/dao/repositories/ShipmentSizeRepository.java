package com.example.selectitdelivery.dao.repositories;

import com.example.selectitdelivery.dao.entity.ShipmentSizeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ShipmentSizeRepository extends JpaRepository<ShipmentSizeEntity,Long> {
}
